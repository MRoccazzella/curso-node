<div>
  <h1 class="title">Lista de productos</h1>

  <table class="product-list">
    <tr>
      <th>Título</th>
      <th>Precio</th>
      <th>Miniatura</th>
      <th>Id</th>

    </tr>
    {{#if products.length}}
      {{#each products}}
        <tr>
          <td>{{this.titulo}}</td>
          <td>{{this.precio}}</td>
          <td><img height="72px" width="72px" src={{this.thumbnail}} /></td>
          <td>{{this.id}}</td>

        </tr>
      {{/each}}
    {{/if}}
  </table>


</div>

<style>
  table, th, td { border: 1px solid; } .title { text-align: center; }
  .product-list { width: 100%; }
</style>